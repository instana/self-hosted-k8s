---
apiVersion: instana.io/v1alpha1
kind: Core
metadata:
  name: instana-core
spec:
  baseDomain: $(BASE_DOMAIN)
  downloadKey: $(DOWNLOAD_KEY)
  ingress:
    - name: "agent-ingress"
      hostName: "$(BASE_URL)"
      port: 443
